.claude/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env

# OS
.DS_Store

# Frontend
node_modules/
docs/
IMPLEMENTATION_PLAN.md

# Eval per-run artifacts (the harness writes timestamped run dirs here; the final versioned
# results report is what gets committed, not every debug run).
eval/runs/